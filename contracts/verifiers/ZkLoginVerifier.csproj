<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>disable</ImplicitUsings>
    <EnableDefaultCompileItems>false</EnableDefaultCompileItems>
    <RunNccsAfterBuild>false</RunNccsAfterBuild>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Neo.SmartContract.Framework" Version="3.8.1" />
  </ItemGroup>

  <ItemGroup>
    <Compile Include="VerifierAuthority.cs" />
    <Compile Include="ZkLoginVerifier.cs" />
  </ItemGroup>

  <Target Name="CompileSmartContract" AfterTargets="Build" Condition="'$(RunNccsAfterBuild)' == 'true'">
    <Message Text="Compiling Neo smart contract: ZkLoginVerifier.cs" Importance="high" />
    <Exec Command="nccs ./ZkLoginVerifier.csproj -o ../bin/v3/verifiers" WorkingDirectory="$(ProjectDir)" />
  </Target>

</Project>
