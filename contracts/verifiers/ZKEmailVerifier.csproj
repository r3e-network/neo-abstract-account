<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>disable</ImplicitUsings>
    <EnableDefaultCompileItems>false</EnableDefaultCompileItems>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Neo.SmartContract.Framework" Version="3.8.1" />
  </ItemGroup>

  <ItemGroup>
    <Compile Include="VerifierAuthority.cs" />
    <Compile Include="ZKEmailVerifier.cs" />
  </ItemGroup>

  <Target Name="CompileSmartContract" AfterTargets="Build">
    <Message Text="Compiling Neo smart contract: ZKEmailVerifier.cs" Importance="high" />
    <Exec Command="nccs ./ZKEmailVerifier.csproj -o ../bin/v3/verifiers" WorkingDirectory="$(ProjectDir)" />
  </Target>

</Project>
