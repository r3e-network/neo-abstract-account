<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <EnableDefaultCompileItems>false</EnableDefaultCompileItems>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>disable</ImplicitUsings>
    <RunNccsAfterBuild>false</RunNccsAfterBuild>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Neo.SmartContract.Framework" Version="3.8.1" />
  </ItemGroup>

  <ItemGroup>
    <Compile Include="VerifierAuthority.cs" />
    <Compile Include="VerifierModels.cs" />
    <Compile Include="NeoNativeVerifier.cs" />
  </ItemGroup>

  <Target Name="CompileSmartContract" AfterTargets="Build" Condition="'$(RunNccsAfterBuild)' == 'true'">
    <Message Text="Compiling Neo smart contract: NeoNativeVerifier.cs" Importance="high" />
    <Exec Command="nccs ./NeoNativeVerifier.csproj -o ../bin/v3/verifiers" WorkingDirectory="$(ProjectDir)" />
  </Target>

</Project>
