<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <EnableDefaultCompileItems>false</EnableDefaultCompileItems>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>disable</ImplicitUsings>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Neo.SmartContract.Framework" Version="3.8.1" />
  </ItemGroup>

  <ItemGroup>
    <Compile Include="HookAuthority.cs" />
    <Compile Include="TokenRestrictedHook.cs" />
  </ItemGroup>

  <Target Name="CompileSmartContract" AfterTargets="Build">
    <Message Text="Compiling Neo smart contract: TokenRestrictedHook.cs" Importance="high" />
    <Exec Command="nccs ./TokenRestrictedHook.csproj -o ../bin/v3/hooks" WorkingDirectory="$(ProjectDir)" />
  </Target>

</Project>
